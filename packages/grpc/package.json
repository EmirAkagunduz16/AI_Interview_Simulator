{
  "name": "@ai-coach/grpc",
  "version": "0.1.0",
  "description": "Shared gRPC definitions and NestJS module for AI Coach microservices",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && mkdir -p dist/proto && cp src/proto/*.proto dist/proto/",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.12.0",
    "@grpc/proto-loader": "^0.7.0",
    "@nestjs/common": "^11.0.0",
    "@nestjs/config": "^4.0.0",
    "@nestjs/microservices": "^11.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "rxjs": "^7.8.1",
    "typescript": "^5.7.2"
  },
  "peerDependencies": {
    "@nestjs/common": "^11.0.0",
    "@nestjs/config": "^4.0.0",
    "@nestjs/microservices": "^11.0.0",
    "rxjs": "^7.0.0"
  }
}
